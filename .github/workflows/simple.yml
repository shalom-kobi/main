---
name: First File Test
on: [push]

jobs:
  run-github-actions:
    runs-on: ubuntu-latest
    steps:

      - name: location
        id: location
        run: |
          pwd
          ls -lah

      - name: Checkout
        id: checkout
        uses: actions/checkout@v3

      - name: list of files
        id: list
        run: |
          pwd
          ls -lah
